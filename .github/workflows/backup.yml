name: MyApp

on:
  push:
    # branches:
    #   - main

jobs:
  backup:
    name: Repository Backup
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
